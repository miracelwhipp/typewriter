<component-set>
    <components>
        <component>
            <role>
                org.apache.maven.lifecycle.mapping.LifecycleMapping
            </role>
            <role-hint>pdf</role-hint>
            <implementation>
                org.apache.maven.lifecycle.mapping.DefaultLifecycleMapping
            </implementation>
            <configuration>
                <lifecycles>
                    <lifecycle>
                        <id>default</id>
                        <phases>
                            <process-sources>
                                ${project.groupId}:${project.artifactId}:${project.version}:preprocess
                            </process-sources>
                            <process-resources>
                                ${project.groupId}:${project.artifactId}:${project.version}:html
                            </process-resources>
                            <compile>
                                ${project.groupId}:${project.artifactId}:${project.version}:pdf
                            </compile>
                            <install>
                                org.apache.maven.plugins:maven-install-plugin:install
                            </install>
                            <deploy>
                                org.apache.maven.plugins:maven-deploy-plugin:deploy
                            </deploy>
                        </phases>
                    </lifecycle>
                </lifecycles>
            </configuration>
        </component>
        <component>
            <role>org.apache.maven.artifact.handler.ArtifactHandler</role>
            <role-hint>pdf</role-hint>
            <implementation>
                org.apache.maven.artifact.handler.DefaultArtifactHandler
            </implementation>
            <configuration>
                <type>pdf</type>
                <extension>pdf</extension>
                <language>html</language>
                <addedToClasspath>false</addedToClasspath>
            </configuration>
        </component>
    </components>
</component-set>